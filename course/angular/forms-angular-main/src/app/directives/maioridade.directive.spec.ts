import { MaioridadeDirective } from './maioridade.directive';

describe('MaioridadeDirective', () => {
  it('should create an instance', () => {
    const directive = new MaioridadeDirective();
    expect(directive).toBeTruthy();
  });
});
